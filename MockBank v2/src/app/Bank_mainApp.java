package app;

import java.util.Locale;
import java.util.Scanner;

public class Bank_mainApp {
    public static void main(String[] args) throws Exception {
        Locale.setDefault(Locale.US);
        Scanner sc = new Scanner(System.in);

        sc.close();
    }
}